# A unique name for the environment. All Trino nodes in a cluster must have the same environment name.
node.environment=production

# A unique identifier for this node within the cluster. Must be unique for every node.
node.id=coordinator-1

# Directory where Trino should store data (like logs, temp files, etc.)
node.data-dir=/var/trino/data
